# pod-display-service.yaml

apiVersion: v1
kind: Service
metadata:
  name: pod-display-service
spec:
  selector:
    app: pod-display
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 80
  type: NodePort
